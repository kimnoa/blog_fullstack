[project]
name = "python-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "aiplatform-git-user-at-431071035110", email = "kang970316@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "crawl4ai>=0.9.2",
    "fastapi>=0.141.1",
]

[project.scripts]
python-project = "python_project:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
